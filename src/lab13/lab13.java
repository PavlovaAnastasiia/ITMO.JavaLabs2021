package lab13;

import com.google.gson.Gson;
import com.google.gson.reflect.TypeToken;

import java.io.UnsupportedEncodingException;
import java.lang.reflect.Type;
import java.net.URLEncoder;
import java.util.*;

public class lab13 {
    //я рассматривала вариант, предложенный в презентации
    //в итоге программа просто выводит строку "Usage: java -jar utility.jar -q query_string"
    //она не считывает адрес с консоли и не выводит список всех мест, которые находятся по этому адресу (кафе, магазины и так далее).
    //подумала, может, добавить такой код, но в итоге при вводе выдает ошибку
    //Scanner scanner = new Scanner(System.in);
    //            System.out.println("По какому адресу вы хотите что-либо найти?");
    //            String query = scanner.nextLine();
    //            scanner.close();
    //            System.out.println("Результат");

}
